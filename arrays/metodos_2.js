const numeros = [1, 2, 3, 4, 5, 8];

//slice remove o elemento de acirod com o paramentro passado
numeros.splice(1, 2);
console.log(numeros);

//pop remove o ultimo elemento do array
console.log(numeros.pop());
console.log(numeros.pop());
console.log(numeros);
